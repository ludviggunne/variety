#include "settings.h"

namespace Settings {

// Initial parameters
	// Camera
	const float CameraInitXRotation = 0.0f;
	const float CameraInitYRotation = 0.0f;
	const float CameraInitDistance  = 10.0f;
	const glm::vec3 CameraInitCenter = glm::vec3(0.0f, 0.0f, 0.0f);

	const float CameraRotateSpeed = 0.5f;
	const float CameraZoomSpeed = 1.0f;
}
